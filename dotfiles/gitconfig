[user]
    name = Chris Linstid
    email = chris.linstid@datadoghq.com
{%@@ if is_workspace @@%}
    signingKey = /home/bits/.ssh/clinstid-datadog-github_id_ed25519.pub
{%@@ else @@%}
    signingKey = /Users/chris.linstid/.ssh/clinstid-datadog-github_id_ed25519.pub
{%@@ endif @@%}

[core]
    excludesfile = ~/.gitignore

[diff]
    tool = vimdiff
    renameLimit = 1000

[difftool]
    prompt = true

[diff]
    tool = nvimdiff

[difftool "nvimdiff"]
    cmd = "nvim -d \"$LOCAL\" \"$REMOTE\""

[merge]
    tool = vimdiff

[mergetool]
    prompt = false

[mergetool "meld"]
    trustExitCode = true
    cmd = open -W -a Meld --args --auto-merge \"$LOCAL\" \"$BASE\" \"$REMOTE\" --output=\"$MERGED\"
[filter "lfs"]
    smudge = git-lfs smudge -- %f
    process = git-lfs filter-process
    required = true
    clean = git-lfs clean -- %f
[pager]
    branch = true
[rerere]
    enabled = true
[push]
    default = simple
[alias]
    rebase = !\"echo NOOOOOOOOOO\"
[fetch]
    prune = true
[pull]
    rebase = false

[url "ssh://git@github.com/DataDog/"]
    insteadOf = https://github.com/DataDog/
[init]
    defaultBranch = main
[commit]
    gpgSign = true
[gpg]
    format = ssh
[url "git@github.com:DataDog"]
    insteadOf = https://github.com/DataDog
